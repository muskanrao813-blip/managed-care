# ── Trino (READ-ONLY — production) ────────────────────────────────────────────
TRINO_HOST=                          # MISSING — need this (e.g. trino.company.internal)
TRINO_PORT=443
TRINO_USER=vasu.verma
TRINO_PASSWORD=                      # put vvaass6543 here, never in code
TRINO_CATALOG=                       # MISSING — e.g. hive or iceberg
TRINO_SCHEMA=                        # MISSING — e.g. managed_care or analytics

# ── LLM ────────────────────────────────────────────────────────────────────────
ANTHROPIC_API_KEY=                   # MISSING

# ── Database — operational write store (Supabase) ──────────────────────────────
SUPABASE_URL=                        # MISSING — create at supabase.com
SUPABASE_ANON_KEY=                   # MISSING
SUPABASE_SERVICE_ROLE_KEY=           # MISSING
DATABASE_URL=                        # MISSING — postgres connection string

# ── WhatsApp (add when provider chosen) ────────────────────────────────────────
WHATSAPP_PROVIDER=                   # WATI | GUPSHUP | TWILIO
WHATSAPP_API_URL=
WHATSAPP_API_KEY=
WHATSAPP_PHONE_NUMBER_ID=

# ── Voice — Exotel ─────────────────────────────────────────────────────────────
EXOTEL_SID=                          # MISSING
EXOTEL_API_KEY=                      # MISSING
EXOTEL_API_TOKEN=                    # MISSING
EXOTEL_CALLER_ID=                    # MISSING — your outbound number
EXOTEL_AGENT_NUMBER=                 # MISSING — number patients call back on

# ── Binah AI ───────────────────────────────────────────────────────────────────
BINAH_API_KEY=                       # MISSING
BINAH_SDK_LICENSE=                   # MISSING

# ── Webhook server ─────────────────────────────────────────────────────────────
WEBHOOK_BASE_URL=http://localhost:8000   # change to ngrok URL or production domain

# ── Escalation / CRM ───────────────────────────────────────────────────────────
SFDC_API_URL=                        # MISSING — or leave blank to use Supabase only
SFDC_API_KEY=                        # MISSING
